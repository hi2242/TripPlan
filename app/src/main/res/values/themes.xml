<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TripPlan" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorOnPrimary">@color/white</item> <!-- 아이콘 색상 설정 -->
        <item name="colorOnSecondary">@color/white</item> <!-- 다른 상태의 아이콘 색상 설정 -->
        <item name="buttonStyle">@style/CustomButtonStyle</item> <!-- 버튼 스타일 설정 -->
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TripPlan" parent="Base.Theme.TripPlan" />
</resources>